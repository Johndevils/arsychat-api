{
  "name": "arsychat-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for Hugging Face Inference API with custom routing",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "wrangler deploy"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
